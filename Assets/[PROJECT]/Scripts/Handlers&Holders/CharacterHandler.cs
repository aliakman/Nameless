using Interfaces;
using Informations;
using UnityEngine;
using Helpers;

public class CharacterHandler/* : *//*Collideable, IDestroyable, IRageable*/
{
    //private ReferenceHolder refHolder;

    //private float tmpRage;
    //public float Rage { get => tmpRage; set => tmpRage = value; }

    //public void TakeRage(float _rage)
    //{
    //    tmpRage += _rage;

    //    if (tmpRage >= refHolder.infoHolder.characterStat.rage)
    //        Destroy();
    //}

    //public void Destroy()
    //{
    //    Utilities.PlayAnimationClip(refHolder.animator, "Die");
    //}

    //private void Update()
    //{
    //    if (tmpRage > 0)
    //        tmpRage -= Time.deltaTime * refHolder.infoHolder.characterStat.rageReduceMul;
    //}

}
